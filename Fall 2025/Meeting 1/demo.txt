# First meeting demo

# Step 1: install Conda
# Create a new environment for all packages used in this group
conda create --name dirp-env
conda activate dirp-env
conda install ipykernel jupyterlab matplotlib numpy

# Step 2: practice Python through command line
# Find where python.exe is
which python
python
import numpy as np
np.zeros(3)
np.zeros([2, 4])
T = np.ones([3, 1, 5])
T.size
T.ndims
T.shape
T.dtype
A = np.array([[1, 1, 1], [2, 2, 2], [3, 3, 3]])
A
x = np.array([4, 5, 6])
x
y = np.array([[4], [5], [6]])
y
z = np.array([[4, 5, 6]])
z
A * x # Not what you think it is
np.matmul(A, x)
A * y # Not what you think it is
np.matmul(A, y)
A * z # Not what you think it is
np.matmul(A, z)
3 * A

# Step 3: access GitHub
# Download a local copy of my repository
git clone https://github.com/ashtonvcole/DiRP.git
cd DiRP/Fall\ 2025/Meeting\ 1

# Step 4: Hello World
vim hello_world.py
# i to insert
# Write your program
# ESC :wq ENTER to exit editing mode, save and quit

# Step 5: run Jupyter to edit Jupyter notebooks
# Make this environment accessible to Jupyter
python -m ipykernel install --user --name=dirp-env
jupyter lab